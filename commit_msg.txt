Fix Forge/Remote API provider bugs + add Forge multipart provider

Bug fixes (Remote API / Forge provider):
- Fix: provider now sent as openai or forge from conn-mode, not defaulting to ollama when config not loaded
- Fix: model_name now included in formData for remote mode (was missing)
- Fix: currentConfig loaded on page init, not just on Config nav
- Fix: /api/config POST now persists provider and api_key (was ignored)
- Fix: toggleApiKeyField shows for both openai and forge

New: Forge multipart provider:
- _call_forge() in ollama_client.py - sends multipart form-data with text_input + files fields
- _build_multipart() - stdlib multipart body builder
- _parse_forge_response() - extracts text from output/response/text fields
- build_forge_text() in prompt_builder.py - combined system+user prompt
- Chat endpoints (/chat, /chat/stream) - forge multipart branch
- Model detection treats forge same as openai (/v1/models endpoint)
- Config page has Forge (multipart API) dropdown option
- All 3 model fetchers use currentConfig?.provider for remote mode
